services:
  - type: web
    name: ventas-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: ventas-db
          property: connectionString

databases:
  - name: ventas-db
    plan: free
